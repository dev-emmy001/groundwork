# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json
#cusor rules
.cursorrules
node_modules
# Local SQLite (src/db/db.ts)
local_database.db*
# Runtime uploads (src/app/api/ingest/route.ts)
uploads/
# nextmodules
.node_modules
.next